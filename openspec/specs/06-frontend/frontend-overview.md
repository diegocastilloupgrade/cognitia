# Frontend overview

La SPA Angular cubre backoffice y shell de ejecución del test. La UI clínica prioriza listados, formularios y resultados; la UI de paciente prioriza el foco en avatar y estímulos.